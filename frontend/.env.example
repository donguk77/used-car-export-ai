# 프론트엔드 환경변수 — Vite 가 빌드 시 읽음.
# 키 prefix 는 반드시 VITE_ — 그래야 클라이언트 번들에 포함됨.
# 실제 값은 .env 또는 .env.local 에 (둘 다 .gitignore).

# 백엔드 API 베이스 URL.
#   - 빈값으로 두면 same-origin (dev 는 vite proxy 가 /api·/health 를 :8000 으로 보냄)
#   - prod 는 백엔드 호스트 지정: https://api.your-domain.com
VITE_API_BASE_URL=
