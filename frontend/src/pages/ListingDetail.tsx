import { PageStub } from "./_PageStub";

export function ListingDetailPage() {
  return (
    <PageStub
      title="거래 상세"
      subtitle="차량 + 바이어 + 통관 결과 + 메일 작성 + 서류 생성 — 한 화면에 통합"
      next="Day 5 — 통합 뷰 (시연의 핵심 화면)"
    />
  );
}
