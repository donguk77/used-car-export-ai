import { PageStub } from "./_PageStub";

export function VehiclesPage() {
  return (
    <PageStub
      title="매물 목록"
      subtitle="등록된 차량 카드 그리드 + 상태 필터 (available / reserved / sold / shipping / delivered)"
      next="Day 2 — 차량 카드 + 필터 칩 + 페이지네이션"
    />
  );
}
