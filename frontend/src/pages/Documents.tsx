import { PageStub } from "./_PageStub";

export function DocumentsPage() {
  return (
    <PageStub
      title="수출 서류 자동 생성"
      subtitle="거래 선택 → 4종 PDF 생성 (Invoice / PL / SI / C/O) → 미리보기·다운로드"
      next="Day 5 — PDF 미리보기 iframe + 다운로드 버튼"
    />
  );
}
