import { PageStub } from "./_PageStub";

export function MailPage() {
  return (
    <PageStub
      title="다국어 메일 작성"
      subtitle="차량 × 바이어 × 시나리오 × 언어 선택 → Gemini 생성 → 미리보기 → 보내기"
      next="Day 5 — 폼 + AI 생성 + 미리보기 패널"
    />
  );
}
