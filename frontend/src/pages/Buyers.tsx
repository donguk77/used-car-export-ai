import { PageStub } from "./_PageStub";

export function BuyersPage() {
  return (
    <PageStub
      title="바이어 목록"
      subtitle="국가별 바이어 + 컴플라이언스 색상 코딩 (clean / warning / blocked)"
      next="Day 4 — 바이어 카드 + 필터 + 컴플라이언스 색상"
    />
  );
}
