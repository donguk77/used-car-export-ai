import { PageStub } from "./_PageStub";

export function SettingsPage() {
  return (
    <PageStub
      title="설정"
      subtitle="프로필 · LLM provider · 기본 통화 · 기본 항구"
      next="Day 6 — 설정 폼"
    />
  );
}
