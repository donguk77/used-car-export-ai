import { PageStub } from "./_PageStub";

export function ListingsPage() {
  return (
    <PageStub
      title="거래 파이프라인"
      subtitle="Kanban: inquiry → quoted → agreed → documenting → shipping → ... → closed"
      next="Day 5 — 칸반 보드 또는 상태별 리스트"
    />
  );
}
